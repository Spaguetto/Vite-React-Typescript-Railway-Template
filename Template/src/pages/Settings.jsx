// components
import Page from '@layout/Page';
import UserSettings from '@widgets/UserSettings';

const Settings = () => {
    return (
        <Page title="Settings">
            <UserSettings/>
        </Page>
    );
}

export default Settings;